# 🕐 Alpha Hive Cron 定时任务配置指南

**完成时间**: 2026-02-24 21:30 UTC
**状态**: ✅ 完全配置
**目标**: 实现每日自动扫描 + Slack 通知

---

## 📋 快速配置（3 步）

### Step 1: 验证脚本
```bash
# 检查脚本是否存在且可执行
ls -lh /Users/igg/.claude/reports/run_alpha_hive_daily.sh
# 输出: -rwxr-xr-x  1 igg staff  ...
```

### Step 2: 测试脚本
```bash
# 手动运行一次，测试是否正常
/Users/igg/.claude/reports/run_alpha_hive_daily.sh

# 查看日志输出
tail -50 /Users/igg/.claude/logs/alpha_hive_*.log
```

### Step 3: 添加到 Cron
```bash
# 编辑 crontab
crontab -e

# 添加一行（参考下面的 Cron 表达式）
```

---

## ⏰ Cron 表达式选项

选择一个适合您的时间：

### 推荐配置

#### 🌅 **早上 8 点** (推荐)
```cron
0 8 * * * /Users/igg/.claude/reports/run_alpha_hive_daily.sh
```
- 工作日前分析前一天收盘后的数据
- 给投资者充足时间做决策

#### 🌆 **中午 12 点**
```cron
0 12 * * * /Users/igg/.claude/reports/run_alpha_hive_daily.sh
```
- 美股中午交易时段
- 实时捕捉中午的市场动向

#### 🌙 **晚上 5 点** (美股收盘)
```cron
0 17 * * * /Users/igg/.claude/reports/run_alpha_hive_daily.sh
```
- 美股交易结束后立即分析
- 捕捉全天的交易数据

#### 🌃 **晚上 8 点**
```cron
0 20 * * * /Users/igg/.claude/reports/run_alpha_hive_daily.sh
```
- 给投资者晚上阅读报告的时间
- 为下一天交易准备

### 其他配置

#### 每小时运行
```cron
0 * * * * /Users/igg/.claude/reports/run_alpha_hive_daily.sh
```
- 小时级监控
- 适合高频交易者

#### 每 4 小时运行
```cron
0 */4 * * * /Users/igg/.claude/reports/run_alpha_hive_daily.sh
```
- 4 小时更新一次
- 平衡监控和资源使用

#### 工作日早上 8 点
```cron
0 8 * * 1-5 /Users/igg/.claude/reports/run_alpha_hive_daily.sh
```
- 仅在工作日（周一到周五）运行
- 周末停止

#### 工作日 8、12、17 点
```cron
0 8,12,17 * * 1-5 /Users/igg/.claude/reports/run_alpha_hive_daily.sh
```
- 工作日的三个关键时段
- 多次更新

---

## 🛠️ 详细配置步骤

### 1. 打开 Crontab 编辑器

```bash
crontab -e
```

根据系统提示选择编辑器（通常是 nano 或 vim）

### 2. 添加 Cron 任务

在文件末尾添加（以早上 8 点为例）：

```cron
# ========================================
# 🐝 Alpha Hive 每日投资研究
# 每天早上 8 点运行蜂群扫描
# ========================================

0 8 * * * /Users/igg/.claude/reports/run_alpha_hive_daily.sh

# 注意:
# - 脚本路径必须是绝对路径
# - 脚本必须有执行权限 (chmod +x)
# - 输出会自动记录到日志文件
```

### 3. 保存并退出

**Nano 编辑器**: 按 `Ctrl+O` 保存，`Ctrl+X` 退出
**Vim 编辑器**: 按 `:wq` 保存退出

### 4. 验证配置

```bash
# 列出所有 cron 任务
crontab -l

# 输出应显示:
# 0 8 * * * /Users/igg/.claude/reports/run_alpha_hive_daily.sh
```

---

## 📊 Cron 表达式说明

```
┌─────────────────────────────────────────────────────┐
│ Cron 表达式格式                                       │
│ ┌──────────┬──────────┬──────────┬──────────┬──────┐  │
│ │ 分 (0-59)│ 小时(0-23)│ 日(1-31)│ 月(1-12)│ 周(0-6)│ │
│ └──────────┴──────────┴──────────┴──────────┴──────┘  │
└─────────────────────────────────────────────────────┘

符号说明:
  *     - 任意时间
  */n   - 每 n 个单位
  ,     - 列表分隔符 (如 8,12,17 表示三个时刻)
  -     - 范围 (如 1-5 表示周一到周五)
  0-6   - 周日(0) 到周六(6)

常用时间表:
  0 8 * * *    - 每天 08:00
  0 */4 * * *  - 每 4 小时
  0 8 * * 1-5  - 工作日 08:00
  0 8,12,17 * * * - 每天 08:00, 12:00, 17:00
```

---

## 📝 日志管理

### 查看日志

```bash
# 查看最新日志
tail -100 /Users/igg/.claude/logs/alpha_hive_*.log

# 查看特定日期的日志
ls -lh /Users/igg/.claude/logs/ | grep "2026-02-24"

# 实时跟踪日志
tail -f /Users/igg/.claude/logs/alpha_hive_*.log
```

### 日志位置

```
/Users/igg/.claude/logs/
├── alpha_hive_2026-02-24_08-00-01.log  ← 每次运行生成一个日志
├── alpha_hive_2026-02-24_12-00-02.log
└── ...
```

### 日志内容示例

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🐝 Alpha Hive 每日扫描 - 2026-02-24_08-00-01
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🚀 启动蜂群扫描...
时间: 2026-02-24 08:00:01

🐝 Alpha Hive 蜂群启动 - 2026-02-24 08:00:01

📊 扫描标的: NVDA, TSLA, MSFT, AMD, QCOM, VKTX

🐝 Alpha Hive 蜂群协作启动 (完全去中心化模式)
📅 日期：2026-02-24
======================================================================

🎯 扫描标的数：6
🐝 蜂群配置：6 个自治 Agent
   ✓ ScoutBeeNova
   ✓ OracleBeeEcho
   ✓ BuzzBeeWhisper
   ✓ ChronosBeeHorizon
   ✓ RivalBeeVanguard
   ✓ GuardBeeSentinel

...

======================================================================
✅ 扫描完成！发现 X 个机会
======================================================================

📊 Top 3 机会:
  1. NVDA: 看多 (8.5/10)
  2. TSLA: 中性 (6.2/10)
  3. MSFT: 看多 (7.8/10)

✅ 报告已推送到 Slack

🐝 蜂群扫描完成！

完成时间: 2026-02-24 08:15:42
退出状态: 0
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## 🔧 常见问题

### Q1: Cron 任务没有执行？

**检查清单**:

```bash
# 1. 验证 crontab 是否正确配置
crontab -l

# 2. 检查脚本权限
ls -l /Users/igg/.claude/reports/run_alpha_hive_daily.sh
# 应该显示: -rwxr-xr-x

# 3. 手动运行脚本测试
/Users/igg/.claude/reports/run_alpha_hive_daily.sh

# 4. 检查 cron 日志
log stream --predicate 'eventMessage contains "cron"' --level debug

# 5. 检查磁盘空间
df -h
```

### Q2: 脚本路径错误？

```bash
# 获取正确的绝对路径
which python3  # 获取 python3 路径
pwd  # 获取当前目录路径

# 确保在 crontab 中使用完整的绝对路径
# ❌ 错误: run_alpha_hive_daily.sh
# ✅ 正确: /Users/igg/.claude/reports/run_alpha_hive_daily.sh
```

### Q3: 权限被拒绝？

```bash
# 添加执行权限
chmod +x /Users/igg/.claude/reports/run_alpha_hive_daily.sh

# 验证权限
ls -l /Users/igg/.claude/reports/run_alpha_hive_daily.sh
# 第一列应该包含 'x': -rwxr-xr-x
```

### Q4: Slack 通知没有收到？

```bash
# 检查 Slack webhook 是否配置
cat ~/.alpha_hive_slack_webhook

# 手动测试 Slack
python3 /Users/igg/.claude/reports/slack_report_notifier.py
```

### Q5: 日志文件太多？

```bash
# 自动清理 30 天前的日志（脚本已内置）
# 或手动清理:
find /Users/igg/.claude/logs/ -name "alpha_hive_*.log" -mtime +30 -delete

# 查看日志大小
du -sh /Users/igg/.claude/logs/
```

---

## 📧 Slack 通知验证

Cron 任务执行时，您应该在 Slack 频道收到：

1. **启动通知** (扫描开始)
   ```
   🚀 Alpha Hive 每日扫描开始
   自动定时任务
   时间: 2026-02-24 08:00:00
   📊 扫描标的: NVDA, TSLA, MSFT, AMD, QCOM, VKTX
   ```

2. **每日报告** (扫描中或扫描后)
   ```
   📰 Alpha Hive 每日投资简报
   📊 发现 N 个投资机会
   [Top 3 机会详情]
   ```

3. **完成通知** (扫描结束)
   ```
   ✅ Alpha Hive 每日扫描完成
   完成时间: 2026-02-24 08:15:42
   ✅ 报告已推送 Slack
   ```

---

## 🎯 最佳实践

✅ **推荐做法**
- 使用绝对路径（`/Users/igg/...`）
- 定期检查日志文件
- 在工作日运行（周一到周五）
- 选择市场活跃时段
- 定期备份 crontab

❌ **避免做法**
- 使用相对路径（`run_alpha_hive_daily.sh`）
- 频率过高导致资源浪费
- 忽视日志错误
- 不检查 Slack 通知
- 在系统负载高峰运行

---

## 📊 建议的 Cron 配置

### 配置 1: 标准（推荐）
```cron
# 每个工作日早上 8 点
0 8 * * 1-5 /Users/igg/.claude/reports/run_alpha_hive_daily.sh
```
✅ 适合大多数投资者

### 配置 2: 多次更新
```cron
# 工作日 8、12、17 点
0 8,12,17 * * 1-5 /Users/igg/.claude/reports/run_alpha_hive_daily.sh
```
✅ 适合活跃交易者

### 配置 3: 全天监控
```cron
# 每小时运行（需要更多资源）
0 * * * * /Users/igg/.claude/reports/run_alpha_hive_daily.sh
```
✅ 适合专业机构

---

## 🔍 监控和维护

### 定期检查
```bash
# 每周检查一次日志
crontab -l  # 验证任务是否还在

# 检查最近的执行情况
ls -lt /Users/igg/.claude/logs/ | head -5

# 查看运行统计
grep -c "扫描完成" /Users/igg/.claude/logs/alpha_hive_*.log
```

### 性能监控
```bash
# 检查脚本执行时间（从日志提取）
grep "完成时间\|启动时间" /Users/igg/.claude/logs/alpha_hive_*.log

# 监控日志大小
watch -n 10 'du -sh /Users/igg/.claude/logs/'
```

---

## 🚨 故障排除流程

```
发现问题
   ↓
检查 crontab: crontab -l
   ↓
手动运行脚本: /Users/igg/.claude/reports/run_alpha_hive_daily.sh
   ↓
查看日志: tail -100 /Users/igg/.claude/logs/alpha_hive_*.log
   ↓
检查权限: ls -l run_alpha_hive_daily.sh
   ↓
测试 Slack: python3 slack_report_notifier.py
   ↓
问题解决 → 重新添加到 crontab
```

---

## 📞 快速参考

| 操作 | 命令 |
|------|------|
| 编辑 crontab | `crontab -e` |
| 查看 crontab | `crontab -l` |
| 删除 crontab | `crontab -r` |
| 查看日志 | `tail -50 /Users/igg/.claude/logs/alpha_hive_*.log` |
| 手动运行 | `/Users/igg/.claude/reports/run_alpha_hive_daily.sh` |
| 清理日志 | `find /Users/igg/.claude/logs/ -mtime +30 -delete` |

---

**版本**: 1.0
**维护者**: Alpha Hive 🐝
**更新时间**: 2026-02-24 21:30 UTC
