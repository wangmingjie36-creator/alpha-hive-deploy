# ==================== GitHub Actions（PAT 无 workflow scope，不提交）====================
.github/

# ==================== Python ====================
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/

# ==================== Runtime Data ====================
# 数据库 + SQLite WAL/SHM（v0.23.7 补充：旧 commit 里有，现在 git rm --cached 解除）
*.db
*.db-shm
*.db-wal
*.sqlite3
*.sqlite3-shm
*.sqlite3-wal

# ML 模型
ml_model*.pkl
ml_model*.json

# 日志（含轮转文件 .log.1, .log.2 等）
*.log
*.log.*
logs/*.log
logs/*.log.*
logs/*.jsonl
logs/*.jsonl.*

# ==================== Cache Directories ====================
cache/
data_cache/
sec_cache/
polymarket_cache/
finviz_cache/
reddit_cache/
earnings_cache/
chroma_db/
backups/
db_backups/

# ==================== Temp / Internal ====================
.swarm_results_*.json
.checkpoint_*.json
realtime_metrics.json
status.json
training_data_extended.json

# v0.23.6: sample-accumulator 产出，不应进入 git（agent_toolbox `git add -A` 会误 stage）
.samples-only-*.json
# 部署 push 日志（每次跑都变，无版本意义）
.gh_pages_deploy_log.jsonl

# ==================== Config Backups ====================
*.backup.*

# ==================== Environment ====================
.env
*.pem
*.key
*credentials*.json
*token*.json

# ==================== Analysis Output (Regenerated) ====================
analysis-*.json

# ==================== IDE ====================
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# ==================== macOS ====================
AppIcon.icns
bee_icon_*.png

# ==================== Test Artifacts ====================
.pytest_cache/
htmlcov/
.coverage
test_report_*.html

# API Key 文件（本地敏感数据）
.alpha_hive_finnhub_key
.alpha_hive_av_key
pheromone_fallback.jsonl
