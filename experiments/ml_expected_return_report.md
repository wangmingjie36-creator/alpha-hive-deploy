# ML 预期收益结构性看多偏斜 —— 诊断与修复

**日期**：2026-08-16 · **版本**：v0.44.1
**复现**：`/usr/local/bin/python3 experiments/ml_expected_return_replay.py`

> ⚠️ **v0.45.326（2026-09-23）口径更正 —— 先读这段**
>
> 本报告 v0.44.1 / v0.44.2 两节里的**「真实 7 日收益」及由它算出的一切**（真实收益分布、
> 偏差、MAE、方向准确率、恒定看多基准、动量 rank-IC、拥挤度四口径表）都是对着
> **SL/TP 截断的 `price_t7`（离场价）** 算的，下文逐处标了 〔截断口径〕。
> 脚本已改读 `close_t7`（T+7 收盘价，`ic_diagnostics.FORWARD_CLOSE_COL["t7"]`）。
> 同一快照前后对照、结论怎么变、什么没变 → 文末「**v0.45.326 追加**」。
>
> **不受影响**（与收益列无关）：旧/新公式的**预测**分布、`crowding.score` 分布（n=1057）、
> `probability` 结构性地板、`generate_deep_v2` 阈值触发率。
>
> 一句话：主结论「修掉了 +8pp 的系统性谎报，没有创造 alpha」**不变且更站得住**；
> 脚本印出的「动量负相关 / 短期反转」与「方向准确率差于恒定看多」**是截断造的，且本在噪音内**。
>
> ⚠️ **v0.45.329（2026-09-23）判定口径更正**：下文 v0.44.1 / v0.44.2 两节与 v0.45.326 追加里的
> **动量 / 拥挤度 rank-IC 都是全样本池化**（跨日期混算，主要在测「哪天涨」），所配的「负相关 / 接近 0」
> 「优于 / 持平 / 差于」判定来自写死的 IC ±0.02 / 准确率 ±1pp，阈值在噪音之下。脚本已改为标准横截面
> 周序列口径 + t(n−1) + 按显著性判定 → 文末「**v0.45.329 追加**」。

---

## 起因

`tests/test_distribution_invariants.py` 报出 `RivalBeeVanguard` 近 12 个扫描日
**95.2% 看多**（bullish 180/189），是所有非看空蜂里最偏的，且不在任何已知问题清单里。

## 根因（三层，逐层实测）

### 第 1 层：`expected_7d` 结构性恒正

`ml_predictor.py` 旧公式：

```python
catalyst_bonus = {"A+":25,"A":20,"B+":15,"B":10,"C":5}.get(quality, 10)  # 恒 ≥5
crowding_penalty = clamp(crowding, 0, 100) * 0.1                         # 恒 ≤10
expected_7d = catalyst_bonus + momentum_bonus - crowding_penalty
```

`catalyst_bonus` 永不为负、`crowding_penalty` 上限 10 ⇒ **唯一能为负的项只有动量**。
要让"预期收益"转负需 5 日跌超 5%（催化剂 A+ 时需跌超 25%）。

**量纲也是错的**：`catalyst_bonus` 是催化剂**质量等级**（5~25 的"分"），却直接与
`momentum_5d`（**百分点**）相加当预期收益。于是"B 级催化剂" = +10 个百分点的
7 日预期收益。这与 ChronosBee「权重表只编码影响多大、不编码影响好坏」同族。

### 第 2 层：`rival_bee.py` 把特征写死成常量

```python
crowding_score=50.0,      # 硬编码 —— 蜂群明明算了真实拥挤度
catalyst_quality="B+",    # 硬编码 → catalyst_bonus 恒为 15
iv_rank=50.0,             # 硬编码
put_call_ratio=1.0,       # 硬编码
market_sentiment=_mom_ml * 5,   # 由动量派生，不是真实情绪
```

代入旧公式得闭式 **`expected_7d = 8.0 + 0.8 × momentum_5d`**，在 1057 个配对样本上
**零反例**。验算：动量中位数 0 → +8.00%（实测中位数 +8.00%）；动量最小 −26.04 →
−12.83%（实测最小值 −12.83）。

**生产里所谓的"ML 预期收益"就是一个截距 +8% 的一元线性式。**

⚠️ 这一层**本次未修**。RivalBee 在 Phase-1 并行，`catalyst_quality`（ChronosBee 域）、
`iv_rank`/`put_call_ratio`（OracleBee 域）在那时点确实拿不到，要改需动阶段结构。
见下方「未做」。

### 第 3 层：`probability` 的中性点全在 0.5 之上

`ml_predictor.py` 旧实现的分项：

| 特征 | 旧形式 | 区间 | 中性点 |
|---|---|---|---|
| crowding | `1.0 − x*0.3` | [0.7, 1.0] | 0.85 |
| iv_rank | `1.0 − x*0.3` | [0.7, 1.0] | 0.85 |
| put_call_ratio | `1.0 − x*0.4` | [0.6, 1.0] | 0.80 |
| volatility | `1.0 − x*0.5` | [0.5, 1.0] | 0.75 |
| momentum / sentiment / agent_agreement / direction_encoded | `0.3 + x*0.7` | [0.3, 1.0] | 0.65 |

按默认权重算出**结构性地板 0.3610**，与实测最小值 **0.3500** 吻合。
实测 `ml.probability` **99.6% > 0.5**，中位数 0.5728。向下空间 0.139 vs 向上 0.45
——**不对称约 3.2 倍**，它无法表达"强烈看空"。

代码注释声称「每个分项必须在 [0,1] 范围内 …… 加权和自然在 [0,1]」——范围确实对，
但**中性点不在 0.5**，这是注释没说、也没人验过的那一半。

### 附带：三处重复 + 一处更露骨的钳制

- 公式在 `SimpleMLModel:390` / `SGDMLModel:672` / `HGBModel:1197` **逐字重复三份**，
  docstring 还写着"公式与 SimpleMLModel 完全一致"——那种一致性靠人肉维护。
  **换模型（Simple→SGD→HGB）对本问题完全无效**，训练只影响 `probability`。
- `ml_predictor_extended.SimpleMLModel`（`ml_predictor` 导入失败时的降级实现）
  用 `max(0, ...)` 让负值**结构性不可能**，且期限缩放是 0.3/**0.7**/1.2，
  与主实现的 0.3/**0.8**/1.2 悄悄不一致。

---

## 修法

1. **催化剂质量只调幅度不调方向**：改为无量纲乘数
   `{"A+":1.5,"A":1.3,"B+":1.0,"B":0.9,"C":0.7}`，不参与符号。
2. **拥挤度以中性 50 为中心**：`tilt = (crowding−50)/50 × 5.0`，可正可负。
3. **统一量纲**：全部项为百分点。⇒ **无信号时预期收益为 0，而不是 +8%。**
4. **单一真相**：三处重复合并为模块级 `expected_returns()`，三个模型类共同委托。
5. **`probability` 居中**：新增 `centered_feature(x, influence, inverse)`，
   **保留每项原有的影响力系数**，只把中性点搬回 0.5。因此不是靠削弱某个特征来
   消除偏斜——特征间相对影响力不变。`influence=1.0` 时等价恒等映射，
   所以本来就居中的 catalyst / final_score / odds_score / risk_adj_score 四项数值不变。
6. **降级实现镜像主实现**，去掉 `max(0,...)`，期限缩放对齐。
   重复是结构性强制的（它存在的前提就是主实现不可用），
   用 `TestFallbackMirrorsPrimary` 断言两份数值一致来兜住。
7. **`rival_bee.py:121-126` 的 EPS 单向棘轮**：`positive` 会把 neutral 翻成 bullish，
   `negative` 缺对应分支。补齐。**必须与本次同时落地**——修好 expected_returns 后
   neutral 会真的出现（动量为 0 的样本在近 12 个扫描日占 63.7%），
   这个棘轮就会接替成为主要偏斜源。
8. **缺失值处理**：`momentum_5d` 为 None/NaN/非数值时按 0（无观点）。
   旧实现 `momentum_bonus = data.momentum_5d` 对 None 会直接 TypeError。

---

## 真实数据回放（697 条配对样本）〔截断口径〕

> 〔截断口径〕本节「真实 7 日收益」行、偏差、MAE、方向准确率、恒定看多基准、动量 IC
> 均按 `price_t7`（SL/TP 离场价）算；两行「预测」分布不受影响。收盘口径重算见文末 v0.45.326 追加。

| | 中位数 | 均值 | p5 | p95 | 为正占比 |
|---|---|---|---|---|---|
| **真实 7 日收益** | −0.47% | +0.20% | −12.04% | +11.99% | **48.4%** |
| 旧公式预测 | +8.29% | +9.25% | +0.73% | +20.03% | **97.1%** |
| **新公式预测** | +0.29% | +1.25% | −7.27% | +12.03% | **51.9%** |

**偏差（预测 − 真实）**：旧 **+9.06pp** → 新 **+1.06pp**
**平均绝对误差**：旧 11.02 → 新 **8.12**（改善 26%）

### 方向准确率（诚实读法）

| | 准确率 | 命中 | 错 | 弃权 |
|---|---|---|---|---|
| 旧公式 | 49.8% | 347 | 350 | 0 |
| 新公式 | 50.0% | 327 | 327 | 43 |
| 对照 · 恒定看多 | 48.4% | — | — | — |

5 日动量 vs 未来 7 日收益 rank-IC = **+0.0079** ≈ 0。

> **本次修复消除了一个 +9pp 的系统性谎报，但没有创造 alpha。**
> 方向准确率 49.8% → 50.0%，在噪音内。这与 MEMORY 里「T+7 横截面选股接近有效
> 市场」「瓶颈在信号层不在加权层」一致。新增的 43 次弃权是进步的一部分：
> 系统现在会诚实地说"没有观点"，而不是报 +8% 看多。

---

## 连带修好的下游近死分支

`generate_deep_v2.py` 的一批阈值触发率（n=1057）：

| 条件 | 旧公式 | 新公式 | 位置 |
|---|---|---|---|
| `ml_7d > 3.0`（ML看多/蜂群看空） | **89.9%** | 21.0% | `:161` |
| `ml_7d < -3.0`（ML看空/蜂群看多） | **1.2%** ← 近死分支 | 18.1% | `:166` |
| `abs(ml_7d) > 1.0` | 96.0% | 66.3% | `:3707` |
| `ml_7d < -1.0` | **2.2%** | 31.2% | `:3834` |
| `ml_7d > 1.0` | 93.9% | 35.1% | `:3836` |

深度报告的「ML vs 蜂群分歧」检测器此前是**结构性单向**的：几乎永远报
"ML 看多、蜂群看空"，而反向那条基本不可能触发。

**这些阈值本身是对称写的（±3.0、±1.0），暴露了作者意图——他假定
`expected_7d` 以 0 为中心。修复让现实对上了意图，所以下游阈值不需要重调。**

---

---

# v0.44.2 追加：传入真实拥挤度，并否决把它做成收益项

## 做了什么

`rival_bee.py` 不再把 `crowding_score` 写死为 50.0，改为走 ScoutBee / GuardBee
**已在用的同一条路**取真实值：

```python
_detector = CrowdingDetector(ticker)
_metrics = get_real_crowding_metrics(ticker, stock, self.board)
_crowding, _ = _detector.calculate_crowding_score(_metrics)
```

**成本可忽略**：内部两个网络调用都有缓存（social TTL 3600s / short interest
TTL 86400s），`stock` 由本蜂已取的 `_get_stock_data` 传入。ScoutBee 先跑时是缓存
命中；最坏情况（与 ScoutBee 并行同时未命中）每标的多一次抓取。
GuardBee 在 Phase-1.5，必然命中。

只补这一个特征的原因：`catalyst_quality` 属 ChronosBee 域、
`iv_rank`/`put_call_ratio` 属 OracleBee 域，而 RivalBee 在 **Phase-1 并行**运行
—— 那个时点它们还没产出。要补需动阶段结构。

## 过程中发现的陷阱：中性点是猜的

v0.44.1 把倾斜的中性点设为 **50**（量表中点）。而实测 `crowding.score`（n=1057）：

| min | p10 | p25 | **中位** | p75 | p90 | max | ≥50 占比 |
|---|---|---|---|---|---|---|---|
| 10.55 | 13.40 | 17.43 | **23.30** | 32.89 | 43.23 | 68.56 | **4.6%** |

检测器自己的档位是 `<30` 低 / `30~60` 中 / `≥60` 高，即设计者心里的中性带中心
约 45 —— **也与实测差得远**，95% 以上样本落在"低拥挤度"档。

**若直接把真实值传入而不重标中性点**：95.4% 的样本会被判"不拥挤 → 加分"，
中位 tilt = −2.67pp ⇒ **重新引入 +2.67pp 的系统性正偏**，把 v0.44.1 修掉的东西
部分抵消回去。所以「传真实值」和「重标中性点」必须同时做——
**这个改动不是"低风险局部改进"，除非两件一起做。**

## 但最终否决了倾斜项本身

重标后（中心 23.30、稳健半宽 14.92、钳 ±5pp）全样本 tilt 中位数 = 0.000pp，
偏差改善到 **+0.19pp**。看着更好，但用现成的四口径工具
（`signal_archive.py --analyze`，噪音地板 |IC|=0.076、需 ≥3/4）复核后否决：

> 〔截断口径〕下表出自 v0.45.321 之前的 `signal_archive --analyze`——那时它也读 `price_t7`，
> v0.45.321 已声明其之前的全部 `--analyze` 输出作废。下方三条否决理由里，2（三重计数）、
> 3（不是收益预测量）不依赖这些数值；1 引用了本表的 IC 符号作「证据方向」，但它的结论是
> 「方向**未确立**」——证据作废只会让这句更成立，不会翻转否决。数字只作历史记录，勿引用。

| 信号 | 样本 | 日度IC | t | 通过口径 | 判定 |
|---|---|---|---|---|---|
| `crowding.score`（连续） | 669 | **+0.1122** | +2.48 | **1/4** | 🟡 口径不足 |
| `crowding.signal` | 669 | −0.1128 | −2.49 | 1/4 | 🟡 口径不足 |
| `crowding.adj_factor`（分档） | 669 | −0.1117 | −2.81 | 3/4 | 🟢 候选 **⚠️稀疏** |

三条否决理由：

1. **方向未确立，且现有证据与设计意图相反。** 三个口径都指向
   「**高拥挤 → 收益更高**」，而检测器 `get_adjustment_factor` 对高拥挤打 30% 折。
   连续版**不达标**（1/4）；达标版（3/4）只有 6 个取值、被工具自己标 ⚠️稀疏，
   命中 MEMORY 记载的「`distinct_ratio<0.25` → rank-IC 失真」陷阱。
   ⇒ 不该据此翻转符号，也不该继续用一个与现有证据相反的符号。
2. **三重计数。** 拥挤度已是 `predict_probability` 里**权重最大的特征**
   （`crowding: 0.18`），又经 `adj_factor` 作用于综合分。
3. **它不是收益预测量。** 拥挤度是容量/风险概念，已确立的用法是乘性折扣；
   折成"多少个百分点的预期收益"需要凭空定标度。

**代价说清**：否决后偏差从 +0.19pp 回到 +1.06pp。但那 +0.19 是**巧合**——
倾斜的均值恰好抵消了动量带来的正偏，不是因为符号对。
**宁要 +1.06pp 的可辩护公式，不要 +0.19pp 的不可辩护公式。**

## 最终回放（697 条）〔截断口径〕

> 〔截断口径〕「真实」行与偏差 / MAE / 方向准确率三列按 `price_t7` 算；「中位 / 均值 / 为正」
> 三列在预测行上不受影响。收盘口径见文末 v0.45.326 追加。

| | 中位 | 均值 | 偏差 | MAE | 为正 | 方向准确率 |
|---|---|---|---|---|---|---|
| 真实 7 日收益 | −0.47 | +0.20 | — | — | 48.4% | — |
| 旧公式（≤0.44.0） | +8.29 | +9.25 | **+9.06** | 11.02 | 97.1% | 49.8% |
| v0.44.1 | +0.29 | +1.25 | +1.06 | 8.12 | 51.9% | 50.0% |
| *[已否决] 拥挤倾斜* | *+0.38* | *+0.38* | *+0.19* | *8.17* | *52.5%* | *49.6%* |
| **v0.44.2 最终** | **+0.29** | **+1.25** | **+1.06** | **8.12** | **51.9%** | **50.0%** |

真实拥挤度**仍然被传入** `TrainingData` —— 它在 `probability` 里是权重最大的
特征，那条路径确实用上了。只是不参与收益预测。

## 未做（需决策）

1. **`catalyst_quality` / `iv_rank` / `put_call_ratio` 仍写死**。要补需动 Phase
   结构（RivalBee 与产出方并行）。当前 `expected_7d = 0.8 × momentum_5d`，
   而 5 日动量 IC=+0.0079〔截断口径；收盘口径 −0.0019，见文末〕
   ⇒ **诚实的表述仍是：RivalBee 不提供方向信息。**
2. **拥挤度的真实方向**值得单独查：三个口径一致指向"高拥挤 → 收益更高"，
   与常识和检测器设计都相反。若成立，`get_adjustment_factor` 的折扣方向也是反的
   ——那是个比本次修复大得多的问题。**建议按 MEMORY 的「稀疏信号必须改用事件
   研究」走，不要用 rank-IC 下结论。**
3. **样本世代**：本次改动会让 `ml.expected_*` 前后不可比。旧样本 1094 条应按
   旧口径单独看待，勿与新样本混算 IC。

## 已知限制

- 回放用的是 `rival_bee` 硬编码特征下的闭式，因此结论只覆盖生产路径；
  若将来传入真实特征，需重跑本脚本。
- 期限缩放（0.3/0.8/1.2）未改，三个期限仍是同一个量的缩放、符号恒同。
  下游 `avg_ret = (expected_7d + expected_30d)/2` 等于 `expected_7d × 1.0`，
  看似平均两个期限实则没有——有测试钉住这一事实，供将来重构参考。

---

---

# v0.45.326 追加：「真实 7 日收益」改用 close_t7 —— 上面的回放数字都是截断口径

**日期**：2026-09-23 · **数据**：生产 `pheromone.db` 的 `sqlite3.backup()` 只读快照
（predictions 1533 行，`checked_t7=1` 1351 行；配对样本 2026-03-10 ~ 2026-09-11）
**复现**：`/usr/local/bin/python3 experiments/ml_expected_return_replay.py [--json]`

## 发生了什么

脚本原写「由 `price_at_predict` 与 `price_t7` 直接算（不用 `return_t7`，后者路径依赖、
被 SL/TP 截断）」。理由对，**列选错了**：`price_t7` 存的是
`backtester._simulate_trade_path` 的 `exit_price`（离场价，v16.0 / 2026-04-15 起），
与 `return_t7` 一样截断。与 `ic_diagnostics`（v0.45.19）、`signal_archive.load_panel`
（v0.45.321）是同一个误解。现改为调用时查 `ic_diagnostics.FORWARD_CLOSE_COL["t7"]`
（= `close_t7`，T+7 收盘价），输出里点名终点列。

配对样本里的污染程度（同一快照）：

- 两列都有值的 1229 对里，**536 对（43.6%）`|price_t7 − close_t7| > 0.01`**：SL 328、TP 140、T7_CLOSE 68
- 配对 SL/TP 行里 `price_t7 == exit_price` 462/505；`close_t7 == exit_price` 1/505
- 另有 8 对 `price_t7` 为空、`close_t7` 已有（05-29、06-05 两天），旧口径丢掉 ⇒ n 1229 → 1237
- **CRWD 两行拆股单位错配**：某次 `close_correction` 把 `price_at_predict` 按 07-02 的 4:1
  拆股复权，`price_t7` 仍是未复权价 ⇒ 旧口径 **+325.4% / +339.8%**（收盘口径 +6.3% / +16.9%）。
  那次复权发生在 **08-12 之后、09-10 之前**（08-12 备份仍是 448.13、09-10 备份已是 112.03；
  具体哪天待验证）。08-16 写本报告时这两行大概率还不是垃圾（08-12 备份未复权，且原报告均值 +0.20
  与「无垃圾」一致）——是之后的入场价修正让旧口径在这两行上更错了。
  〔v0.45.338 更正：原写「09-15 的 close_correction」，是从备份文件名 `bak_close_correction_20260915`
  推的，而那份备份里 CRWD 早已复权。〕

截断怎样扭曲「真实收益」（两个方向，部分互相抵消）：

- **右尾被砍**：止盈档约 +10%，而 T+7 收盘真实走到 +68%（RKLB 05-05）、+45%（CRCL 04-30）、
  +40%（QCOM 04-29）。p95 +10.85 → **+16.85**。
- **先破止损再收涨**：49 对截断口径 ≤0 而收盘 >0（其中 41 对是看多单破止损后收回），
  29 对反之 ⇒ 净 20 对「真涨」被记成「跌」。恒定看多基准因此被压低：同一批 1229 行上
  截断口径 50.6%、收盘口径 **52.2%**（全样本 1237 行的 52.1% 还多含 8 行，不是同一批）。
- **止损档也把左尾截短了**（方向与上两条相反）：收盘跌 −26% 的记成止损档 −15%（RKLB 06-15 等）。
- 均值（同一 1229 行、CRWD 两行单列）：截断**净**压低约 **0.85pp**——压低的 583 行合计 −1.63pp、
  抬高的 523 行合计 +0.78pp；CRWD 两行比各自收盘口径多出约 **+0.52pp**。
  收盘均值 +1.22 − 0.85 + 0.52 ≈ 旧均值 +0.89：看着「正常」，其实是两个错部分抵消。
  〔v0.45.338 更正：原写「右尾被砍把它压低约 0.87pp」「CRWD 抬高约 +0.54pp」「50.6% → 52.1%」——
  0.87 是两尾的**净**效应不是右尾单独的；0.54 是相对「剔掉这两行」而非相对它们的收盘值；
  50.6/52.1 混了两个不同行集。〕

## 同一快照，全样本前后对照

| | 旧（price_t7，截断） | 新（close_t7） |
|---|---|---|
| 配对样本 | 1229 | 1237 |
| 真实收益 中位 / 均值 | +0.12 / +0.89 | +0.34 / +1.18 |
| 真实收益 p5 / p95 / max | −11.56 / +10.85 / **+339.8** | −11.74 / +16.85 / +68.3 |
| 真实为正（= 恒定看多准确率） | 50.6% | 52.1% |
| 偏差 旧公式 / v0.44.2 最终 | +8.05 / +0.05 | +7.73 / **−0.27** |
| MAE 旧公式 / v0.44.2 最终 | 10.52 / 7.36 | 10.47 / 7.42 |
| *[已否决] 拥挤倾斜 偏差 / MAE* | *+0.19 / 7.51* | *−0.15 / 7.66* |
| 方向准确率 旧公式（命中/错/弃权） | 51.7%（636/593/0） | 52.8%（653/583/1） |
| 方向准确率 v0.44.2 最终 | 49.1%（554/574/101） | 51.1%（580/555/102） |
| v0.44.2 − 恒定看多 | −1.50pp ⇒ 印「**差于**」 | −0.96pp ⇒ 印「持平」 |
| 动量 rank-IC（全样本池化） | −0.0252 ⇒ 印「⚠️ 负相关，短期反转」 | −0.0019 ⇒ 印「➖ 接近 0」 |
| 拥挤度 rank-IC（全样本池化） | +0.0225 | +0.0617 |

预测侧（旧公式 / v0.44.1 / v0.44.2 的分布）只依赖动量、不依赖收益列，只因多了 8 行略动。

## 脚本印出的两条结论都翻了 —— 但两条翻转都不是新证据

1. 动量判定：「⚠️ 负相关 —— 存在短期反转效应」→「➖ 接近 0」
2. 方向准确率：「**差于**恒定看多（−1.5pp）」→「持平（−0.96pp）」

两条都是跨过脚本写死的阈值（IC ±0.02、准确率 ±1pp）翻的，而这两个阈值**都在噪音之下**。
动量用标准横截面口径复核（同 `final_score_dilution.py`：日度横截面 rank-IC → 每 ISO 周取第一天 →
对周序列做 t(n−1) 检验）；准确率按周合并计算后同样做 t 检验：

| | 旧（price_t7） | 新（close_t7） |
|---|---|---|
| 动量 日度横截面 IC 均值（78 天） | −0.064 | −0.037 |
| 动量 周序列（每周第一天，23 周） | −0.021（t=−0.33，p=0.75） | +0.016（t=+0.29，p=0.77） |
| 周度「v0.44.2 准确率 − 恒定看多」均值〔周内合并，见下注〕 | +0.8pp（t=+0.13，p=0.90，23 周） | −2.0pp（t=−0.30，p=0.77，24 周） |

〔v0.45.329 注〕上表第三行是**周内合并**口径（每周把几天的行合在一起算准确率差）——与下面自我更正里
动量 IC 踩的是同一个「只去一半池化」。同日配对口径（每天同一批行上相减 → 每周第一天 → t(22)）：
收盘口径 +0.01pp（p=1.00，23 周）。结论（噪音）不变，数换了；见文末 v0.45.329 追加。

- **两条前后都是噪音**——「负相关/短期反转」「接近 0」「差于」「持平」都不是能读的结论。
- 截断确实把日度平均反转放大了（−0.037 → −0.064，旧口径约 40% 的表观反转是截断造的），
  方向与 v0.45.321 在 `signal_archive` 上看到的一致（`price.momentum_5d` 日度 IC −0.127 → −0.020；
  那边样本不需要 crowding 配对、且按世代切，量级不可直接比）。但**无论哪一列，都没有可用的反转证据**。
- ⚠️ 起草本节时我先用了「每周内把 5 天池化算一个 IC」的口径，得出旧口径 −0.108（p=0.05，「边缘显著」）
  → 新口径 −0.066（p=0.25）。**那个「边缘显著」本身就是周内池化造的**，换成标准横截面口径即消失。
  池化陷阱在复核池化陷阱时又踩了一次——记在这里，别再引用那组数。
- 脚本自己的全样本池化 IC（−0.025 → −0.002）同样混进了时序成分（memory「横截面池化陷阱」）。
  **本次不改**脚本的 IC 口径与阈值——那是另一件事，一起改就不再是「只换列」的前后对照。
  （v0.45.329 已改，见文末。）

## 什么没变

- **主结论不变，且更站得住**：v0.44.1 修掉的是一个约 +8pp 的系统性谎报（旧公式偏差 +7.73pp），
  **没有创造 alpha**；RivalBee 在 `expected_returns` 这条路上不提供方向信息（方向 = sign(动量)，
  动量 IC ≈ 0）。
- **v0.44.2 否决拥挤倾斜不变**：否决依据是「符号不可辩护」，不是偏差数值。换列后二者偏差都在
  ±0.3pp 内（最终 −0.27 / 倾斜 −0.15），「倾斜偏差更小是巧合」这一判断不受影响。
- 本节**只作记录，不据此改任何权重或模型代码**。

## 原报告 697 条：同一行集换列重算

原报告的 697 条能按行集复原——今天的快照上 `date ≤ 2026-08-07` 的 `price_t7` 口径配对恰为 697、
v0.44.2 弃权恰为 43、p5 −12.04 与原报告逐位相同，可确认原报告就是这一列、这一批样本。
但**数值不能逐位复现**：之后的 `close_correction` 改写了部分 `price_at_predict`
（CRWD 两行即其一，08-12 ~ 09-10 之间复权；v0.45.338 更正，原写「09-15」）。三列并排，只为说明「同一批样本换列后结论怎么走」：

| 同一行集 | 原报告（08-16 · price_t7） | 今天快照 · price_t7 | 今天快照 · close_t7 |
|---|---|---|---|
| n | 697 | 697 | 705 |
| 真实 中位 / 均值 | −0.47 / +0.20 | −0.50 / +1.12¹ | +0.19 / +1.36 |
| 真实 p5 / p95 | −12.04 / +11.99 | −12.04 / +12.06 | −14.06 / +20.71 |
| 真实为正（恒定看多） | 48.4% | 48.2% | 50.5% |
| v0.44.2 偏差 | +1.06 | +0.14¹ | −0.16 |
| 方向准确率 旧公式 / v0.44.2 | 49.8% / 50.0% | 49.6% / 50.2% | 51.8% / 50.7% |
| 动量 rank-IC（池化） | +0.0079 | +0.0100 | −0.0019 |

¹ 含 CRWD 两行拆股垃圾（08-16 时尚不存在），均值与偏差被拖偏约 1pp。

原报告「新公式 50.0% 略好于恒定看多 48.4%」在收盘口径下是 50.7% vs 50.5%——
**那 1.6pp 的领先大半是截断把基准压低造成的**。「没有创造 alpha」反而更站得住。

## 回归守卫

`tests/test_ml_expected_return_replay_close_target.py`（9 条）。核心夹具让两列给出**相反的**
动量 IC 与方向准确率（动量为正的票先破止损再收涨、为负的先冲止盈再收跌）。拿改动前的脚本
原样来跑：结论层两条红（`ic_momentum_vs_forward=−0.863`、文本印出「短期反转」），CRWD 同形夹具
读出 +325.36%。另钉住：`price_t7` 为空的行不被过滤；终点列调用时查 `FORWARD_CLOSE_COL`
（写死 `'close_t7'` 或冻结到模块层都会红）。五个变异全部真跑，台账见 CHANGELOG v0.45.326。

---

# v0.45.329 追加：信号强度改为标准横截面周序列口径，判定改按显著性

**日期**：2026-09-23 · **数据**：生产 `pheromone.db` 的 `sqlite3.backup()` 只读快照
（predictions 1533 行，`checked_t7=1` 1351 行——与 v0.45.326 同一批，生产库当天 02:57 后未再写入；
配对样本 1237 条，2026-03-10 ~ 2026-09-11——最晚一天早于维度 IC 预注册窗口起点 `dim_ic_protocol.FORWARD_START`，不含窗口内数据）
**复现**：`/usr/local/bin/python3 experiments/ml_expected_return_replay.py [--json]`

## 改了什么

| | ≤ v0.45.328 | v0.45.329 |
|---|---|---|
| IC 估计量 | 全部日期 × 全部标的一次性 Spearman（**池化**） | 日度横截面 rank-IC（当日 ≥ 5 只）→ 每 ISO 周第一个可用交易日 → 周序列（照抄 `final_score_dilution.weekly_ic`） |
| p 值 | 无 | t(n−1)（`final_score_dilution` 那边是 erfc 正态近似，这里不抄） |
| IC 判定 | IC < −0.02 ⇒「⚠️ 负相关 —— 短期反转」；> +0.02 ⇒「✅ 正相关」；否则「➖ 接近 0」 | p < 0.05 才判「显著正 / 负相关」；否则「➖ 未检出方向信息」（**不是**「接近 0」） |
| 准确率判定 | 全样本「v0.44.2 − 恒定看多」±1pp ⇒ 优于 / 持平 / 差于 | 同一天、同一批行上配对相减 → 同一条周序列 t 检验；不显著印「未检出差异」，明说「≠ 持平」 |
| 池化 IC | 唯一输出 | 保留为 `pooled_across_dates`，文本标「只作对照、不参与判定」 |
| JSON 键 | `ic_momentum_vs_forward` / `ic_crowding_vs_forward` | 删除旧键（旧读者该 KeyError，不该静默读到另一个量）；新 `momentum_ic` / `crowding_ic` / `new_accuracy_edge_vs_always_bullish` |

## 同一快照，新口径

| | 周序列（23 周） | 日度横截面均值（重叠，仅参照） | 全样本池化（仅对照） | 判定 |
|---|---|---|---|---|
| 5 日动量 | +0.0158（t=+0.29，**p=0.775**） | −0.0369（78 天） | −0.0019 | ➖ 未检出方向信息 |
| 拥挤度 | +0.1161（t=+2.06，**p=0.052**） | +0.1254（82 天） | +0.0617 | ➖ 未检出方向信息 |
| v0.44.2 准确率 − 恒定看多（同日配对） | +0.01pp（t=+0.00，**p=1.00**） | −0.09pp（75 天） | −0.96pp | ➖ 未检出差异 |

旧脚本在同一快照上印的是「➖ 接近 0」与「持平」——文字碰巧没翻，但那是跨过 ±0.02 / ±1pp 碰出来的，
不是检验出来的。v0.45.326 前后对照已经证明：同样不显著的数，换一列就能把两条判定一起翻过去。

读法：

- **动量**：周序列、日度均值、池化三个数连符号都不一致（+0.016 / −0.037 / −0.002），这本身就是
  「在噪音里」的样子。没有反转证据，也没有正向证据；「sign(动量) 作方向」这件事在本样本上无法判断好坏。
- **拥挤度 p=0.052 —— 恰好是 t 与正态分叉的地方**：正态近似下同一个 t=2.06 给 p=0.040，
  若照抄 `final_score_dilution.stat` 的 erfc，这一行会印「✅ 显著正相关」。t(22) 下不显著。
  另有三条理由不把它读成信号：①本脚本三条检验、未做族校正（Bonferroni 下 α=0.0167）；
  ②「每周取哪天」这个任意选择对 p 的影响可以远大于 z→t（memory `alpha-hive-t-vs-normal-p`）——
  **本次没有、也不应该**去换别的星期几找显著，那是研究者自由度；③样本跨世代混算。
  顺带：点估计的方向与 v0.44.2 否决掉的拥挤倾斜所假设的方向相反（那个方案里拥挤度越高、预期收益越低），
  与「符号不可辩护」的否决理由一致——但同样只是点估计，不是证据。
- **准确率**：同日配对后，v0.44.2 相对恒定看多的优势是 +0.01pp（23 周，p=1.00）。v0.45.326 追加里
  「按周合并」得出的 −2.0pp（p=0.77）是周内合并口径，现已替换；两者都是噪音，结论不变。

## 什么没变

- 主结论不变：v0.44.1 修掉约 +8pp 的系统性谎报（旧公式偏差 +7.73pp → −0.27pp），**没有创造 alpha**。
  现在这句话由脚本自己的检验直接给出（「没有证据表明带来了方向上的改善」），不再依赖跨阈值。
- v0.44.2 否决拥挤倾斜不变。
- 本节**只作记录，不据此改任何权重或模型代码**。拥挤度那条 p=0.052 尤其不能当作接回拥挤度的理由。
- 维度 IC 预注册（`dim_ic_preregistration.md`；窗口起点唯一真相 `dim_ic_protocol.FORWARD_START`，写作当天由 v0.45.330 修订 1 从 09-28 推迟到 10-12，脚本调用时读该常量、自动跟随）：本样本全部早于窗口起点。
  脚本自 v0.45.329 起在样本含窗口内日期时会印一行提示——那时它的输出只作探索、不构成该协议的证据，
  也不该为「看趋势」反复跑（协议 §8）。
- 二次检查补记：`price.momentum_5d` 恰为 0.0 的 101 行是取数失败的占位（memory「静默降级」`cache.py` fallback），
  其中 06-15（6/9）、06-29（8/9）两个周一被选作所在周的代表日。把 0.0 当缺失剔除后动量周序列
  +0.016（p=0.775）→ +0.004（p=0.943），判定不变；准确率差不受影响（模型在 0 动量上本就弃权）。
  脚本照抄标准口径、未剔除——剔不剔是样本定义问题，不在本次范围。

## 回归守卫

`tests/test_ml_expected_return_replay_cross_sectional.py`（11 条）+ 更新后的
`tests/test_ml_expected_return_replay_close_target.py`（9 条；原 6 天夹具只跨 2 个 ISO 周、
周序列永远「无法判定」，改为 6 周 × 每周 2 天）。

核心夹具让**池化与同日横截面符号相反**：每天有一个大的公共水平——动量整体抬高、收益整体压低——
日内再由小噪音决定排序、方向相反。池化 ⇒ 动量 IC 强负、拥挤度强正；同日横截面 ⇒ 反过来。
每周 3 天、公共水平拉开 ⇒ 「周内合并」也给出池化的符号。另有噪音夹具（池化 IC +0.14、准确率差 +7pp，
都跨过旧阈值；周序列 p≈0.7）和一条 t 与正态分叉的序列（n=5、t=2.5：t(4) p=0.067、正态 p=0.012）。

变异全部真跑（Python 驱动、`PYTHONDONTWRITEBYTECODE=1`、每轮删 pyc），台账见 CHANGELOG v0.45.329。
要点：**改动前的原文件**（`git show HEAD:`）在两条文本判定上红，理由是断言本身——印出了「短期反转」
与「✅ 正相关」，而不是新键不存在。
